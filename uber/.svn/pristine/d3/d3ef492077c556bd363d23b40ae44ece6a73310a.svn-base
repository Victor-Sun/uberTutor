Ext.define('ExtApp.store.rolemanage.Roles', {
    extend: 'Ext.data.Store',
    alias: 'store.roles',
    fields: ['title', 'titleEn', 'internalUser','isSysRole'],
    proxy: {
    	type: 'ajax',
        url: '/pdms/role/role!getRoleList.action',
        reader: {
            type: 'json',
            rootProperty: 'data'
        }
    },
    autoLoad: true
});